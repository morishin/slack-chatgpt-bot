export const env = {
  SLACK_APP_NAME: "gpt-bot",
  SLACK_APP_DISPLAY_NAME: "gpt-bot",
  // A system message for ChatGPT (configurable by users after installation)
  INITIAL_SYSTEM_MESSAGE: "You are a helpful assistant.",
  // Channel ID where ChagGPT bot works (configurable by users after installation)
  INITIAL_SLACK_CHANNEL_ID: "CXXXXXXXXX",
  // Slack team ID used for chat streaming APIs
  SLACK_TEAM_ID: "TXXXXXXXXX",
  // Timeout for chaining responses via previous_response_id (minutes)
  RESPONSE_CHAIN_TIMEOUT_MINUTES: 30,
  // Soft-timeout guard for StreamReplyFunction (milliseconds)
  FUNCTION_TIMEOUT_ALERT_MS: 58_000,
  // Enable OpenAI Responses web search tool
  OPENAI_ENABLE_WEB_SEARCH: true,
  // Enable OpenAI Responses code interpreter tool
  OPENAI_ENABLE_CODE_INTERPRETER: true,
  GPT_MODEL: "gpt-5-nano",
};
